---
M_1:
  base_file: EoSense_sync.yaml
  target: SNR

M_2:
  base_file: EoSense_sync.yaml
  sweep_params:
    - "['averages']"
  sweep_vals:
    - "5"
  target: SNR

M_3:
  base_file: EoSense_sync.yaml
  sweep_params:
    - "['static_devices']['mw_source']['channels']['channel_1']['frequency']"
  sweep_vals:
    - "4.71;4.72;5"
  target: SNR
  functions:
    - log(x) #log(x)  #x or linear both work for linear functions

M_4:
  base_file: EoSense_sync.yaml
  sweep_params:
    - "['pulse_sequence']['pi']"
    - "['pulse_sequence']['pi_half']"
  sweep_vals:
    - "20;30;4"
    - "40;60;6"

  target: SNR


M_5:
  base_file: EoSense_sync.yaml
  sweep_params:
    - "['pulse_sequence']['readout_time']"
    - "['pulse_sequence']['laserduration']"
    - "['averages']"
  sweep_vals:
    - "1;20;5"
    - "5;30;5"
    - "5"
  functions:
    - x**2
    - log(x)
    - linear
  target: SNR

  # linspace aufbauen und höchste anzahl an schritten, die man verwendet
